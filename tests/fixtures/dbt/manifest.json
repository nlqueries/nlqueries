{
  "metadata": { "dbt_schema_version": "https://schemas.getdbt.com/dbt/manifest/v11.json" },
  "nodes": {
    "model.shop.orders": {
      "resource_type": "model",
      "name": "orders",
      "relation_name": "\"analytics\".\"finance\".\"orders\"",
      "description": "One row per confirmed customer order.",
      "columns": {
        "order_id": { "name": "order_id", "description": "Surrogate key for the order." },
        "customer_id": { "name": "customer_id", "description": "FK to the customers model." },
        "amount": { "name": "amount", "description": "" },
        "status": { "name": "status", "description": "   " }
      }
    },
    "model.shop.customers": {
      "resource_type": "model",
      "name": "customers_raw",
      "alias": "customers",
      "description": "Deduplicated customer master.",
      "columns": {
        "customer_id": { "name": "customer_id", "description": "Customer primary key." }
      }
    },
    "seed.shop.country_codes": {
      "resource_type": "seed",
      "name": "country_codes",
      "description": "Not a model — must be ignored.",
      "columns": {}
    },
    "test.shop.some_test": {
      "resource_type": "test",
      "name": "not_null_orders_order_id"
    }
  }
}
